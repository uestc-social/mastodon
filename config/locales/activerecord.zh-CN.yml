---
zh-CN:
  activerecord:
    attributes:
      poll:
        expires_at: 截止日期
        options: 选项
      user:
        agreement: 服务协议
        email: 邮件地址
        locale: 语言区域
        password: 密码
      user/account:
        username: 用户名
      user/invite_request:
        text: 理由
    errors:
      attributes:
        domain:
          invalid: 不是一个有效的域名
      messages:
        invalid_domain_on_line: "%{value} 不是一个有效的域名"
      models:
        account:
          attributes:
            fields:
              fields_with_values_missing_labels: 包含缺少标签的值
            username:
              invalid: 只能包含字母、数字和下划线
              reserved: 已被保留
        admin/webhook:
          attributes:
            url:
              invalid: 不是一个有效的 URL
        doorkeeper/application:
          attributes:
            website:
              invalid: 不是一个有效的 URL
        import:
          attributes:
            data:
              malformed: 格式错误
        list_account:
          attributes:
            account_id:
              taken: 已在列表中
          must_be_following: 必须是已关注的帐户
        status:
          attributes:
            reblog:
              taken: 的贴文已存在
        terms_of_service:
          attributes:
            effective_date:
              too_soon: 时间太早，必须晚于 %{date}
        user:
          attributes:
            date_of_birth:
              below_limit: 低于年龄限制
            email:
              blocked: 使用了不允许的邮件提供者
              unreachable: 似乎不存在
            role_id:
              elevated: 不能高于你当前的身份组
        user_role:
          attributes:
            permissions_as_keys:
              dangerous: 包含对于基础身份组不安全的权限
              elevated: 不能包含你当前身份组不拥有的权限
              own_role: 无法用你当前的身份组进行更改
            position:
              elevated: 不能高于你当前的身份组
              own_role: 无法用你当前的身份组进行更改
        webhook:
          attributes:
            events:
              invalid_permissions: 不能包含你无权访问的事件
